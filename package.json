{
  "name": "rmt-compose-poc",
  "version": "1.0.0",
  "type": "module",
  "description": "Relative Music Theory Compose - Proof of Concept",
  "homepage": "https://www.rmt.world/",
  "scripts": {
    "dev": "vite",
    "dev:wasm": "npm run wasm:build && vite",
    "build": "npm run wasm:build && vite build",
    "preview": "vite preview",
    "wasm:build": "cd rust && wasm-pack build --target web --out-dir pkg",
    "wasm:dev": "cd rust && wasm-pack build --target web --dev --out-dir pkg",
    "wasm:test": "cd rust && wasm-pack test --headless --chrome",
    "wasm:clean": "rimraf rust/pkg rust/target"
  },
  "dependencies": {
    "fraction.js": "^4.3.7"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  },
  "author": "Cyril Monkewitz",
  "license": "LicenseRef-RMT-PNC-1.0"
}
